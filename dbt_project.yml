name: dbt_labs_takehome
version: 1.0.0
config-version: 2

profile: dbt_labs_takehome

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_labs_takehome:
    staging:
      +materialized: view
      +tags: ["prod"]
    intermediate:
      +materialized: view
      +tags: ["prod"]
    marts:
      +materialized: table
      +tags: ["prod"]

seeds:
  dbt_labs_takehome:
    +schema: raw
    +tags: ["prod"]
